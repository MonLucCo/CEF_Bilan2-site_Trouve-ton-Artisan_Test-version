import { TopFilterPipe } from './top-filter.pipe';

describe('TopFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TopFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
